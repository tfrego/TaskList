<h2> New Task! </h2>

<%= form_with model: @task do |f| %>
  <%= f.label :name, "Task"%>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.submit "Save Task", class: "book-button" %>
<% end %>
